import 'package:flutter/widgets.dart';


class Standings extends StatelessWidget {
  final Color color;
  

  Standings(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
